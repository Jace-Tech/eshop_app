import { StyleSheet } from 'react-native'

export const globalStyles = StyleSheet.create({
    center: {
        alignItems: 'center', 
        justifyContent: 'center',
    },

    centerVertical: {
        flexDirection: 'row',
        alignItems: 'center',
    }
})